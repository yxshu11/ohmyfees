<% provide(:title, @staff.name) %>

<div class="container">
<h1>Account Information:</h1>
  <div class="row">
    <div class="col-md-6 col-md-offset-3 well">
			<h5>
      <section class="center">
        <% if @staff.picture.blank? %>
          <%= image_tag("default_profile_picture.png", width: '120', :class => "img-circle") %>
          <br><br>
          <%= link_to "Update your profile picture", edit_staff_path(current_user) %><br><br>
        <% else %>
          <%= image_tag(@staff.picture_url(:standard)) %>
        <% end %>
      </section>
			<table class="table noborder">
        <tr>
          <td class="no-repeat-header"><b>Name:</b></td>
          <td><%= @staff.name %></td>
				</tr>
				<tr>
          <td><b>Staff Number:</b></td>
					<td><%= @staff.staff_number %></td>
				</tr>
				<tr>
          <td><b>Email:</b></td>
					<td><%= @staff.email %></td>
        </tr>
        <tr>
          <td><b>Contact Number:</b></td>
					<td><%= @staff.contact_number %></td>
        </tr>
				<tr>
					<td></td>
					<td></td>
				</tr>
			</table>
      </h5>
			<section class="center">
				<%= link_to "Edit", edit_staff_path(@staff), class: "btn btn-primary" %>  &nbsp;
				<%= link_to "Back", dashboard_path, class: "btn btn-default" %>
			</section>
		</div>
	</div>
</div>
